{
  "name": "hyperreduce",
  "version": "1.0.0",
  "description": "Distributed reduce on top of hypercore",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/hyperreduce",
  "keywords": [
    "hyper",
    "reducer",
    "map-reduce",
    "hypercore",
    "dat"
  ],
  "license": "MIT",
  "dependencies": {
    "hypercore-index": "^5.1.0"
  },
  "devDependencies": {
    "dependency-check": "^2.6.0",
    "hypercore": "^4.11.0",
    "istanbul": "^0.4.5",
    "memdb": "^1.3.1",
    "standard": "^8.5.0",
    "tape": "^4.6.2"
  }
}
